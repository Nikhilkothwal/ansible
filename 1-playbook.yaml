- name: install nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.bulitin.package:
      name: nginx
      state: present

  - name: start nginx
    ansible.bulitin.service:
      name: nginx
      state: started
      enabled: yes